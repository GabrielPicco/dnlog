# ============================================================
# DNLog Backend — Configuração de ambiente
# ============================================================
# Copie este arquivo para .env e ajuste os valores conforme seu ambiente

# Porta do backend (onde o DNLog vai conectar)
PORT=3000

# ----- MODO DE OPERAÇÃO -----
# true  = usa mock do SAP (dados fake, ideal pra desenvolvimento)
# false = conecta no Service Layer SAP REAL (precisa Agrotis configurar acessos)
USE_MOCK=true

# ----- CONFIGURAÇÕES DO SAP SERVICE LAYER (só usado se USE_MOCK=false) -----
# URL do Service Layer fornecida pela Agrotis
SAP_SERVICE_LAYER_URL=https://homolog.skyone.solutions:50000/b1s/v1

# Nome da CompanyDB (fornecido pela Agrotis)
SAP_COMPANY_DB=SBO_DNSEEDS_HML

# Credenciais do usuário dnlog_service (criado pela Agrotis)
SAP_USERNAME=dnlog_service
SAP_PASSWORD=alterar_aqui

# Se o SAP usar certificado autoassinado em homologação, deixe true
SAP_IGNORE_SSL_ERRORS=true

# ----- CORS -----
# URL do frontend que vai consumir esse backend (DNLog)
# Use * em desenvolvimento; em produção, especifique o domínio
CORS_ORIGIN=*
